/**
 * ARQUIVO GERADO AUTOMATICAMENTE — não edite à mão.
 *
 * Criado por `scripts/gerar-vagas.mjs`, que roda antes de cada build.
 * Para regenerar: `npm run vagas` (ou simplesmente `npm run build`).
 */

export const VAGAS_GERADAS = {
  "geradoEm": null,
  "fonte": "exemplos",
  "motivo": "JOOBLE_API_KEY não configurada — sem fonte de vagas reais.",
  "total": 0,
  "vagas": []
}
